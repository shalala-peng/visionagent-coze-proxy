# VisionAgent × Coze 智能体配置清单（可直接粘贴进扣子）

> 用途：在 Coze（扣子）上搭一个**真能跑**的「视觉内容生产线规划助手」，把产品里最虚的"自然语言 → 生产线"那层做成真实可调用的能力（非 mock）。
> 输出结构与我们 `visionagent-prototype.html` 的画布数据模型对齐，方便后续把 Coze 生成的 JSON 直接灌进原型画布。
> 注意：**Coze 负责"大脑逻辑"，不负责"画界面"**。本配置产出的结果是一段结构化 JSON，不是 11 页 UI。

---

## 一、智能体基础信息（创建智能体时填写）

| 项 | 内容 |
|---|---|
| 名称 | VisionAgent 生产线规划助手 |
| 简介 | 输入一句视觉内容生产需求，自动拆任务、匹配技能、输出可执行的智能体生产线 JSON |
| 头像/图标 | 任意（建议用相机/流水线图标） |
| 模型 | 选当前最强大模型（如豆包·Pro / GPT-4o 类），保证拆解质量 |
| 开场白 | "你好，描述你想自动化的视觉内容生产流程，我来帮你拆成可执行的智能体生产线。" |

---

## 二、人设与回复逻辑（粘贴到「人设 / 回复逻辑」框）

```text
你是 VisionAgent 的"生产线规划助手"，服务于视觉内容团队（电商、摄影、餐饮、地产、广告、自媒体）。

你的唯一任务：把用户用自然语言描述的需求，转化为一条"智能体生产线"的结构化 JSON。

规则：
1. 先判断行业（电商/摄影/餐饮/地产/广告/旅拍/自媒体/活动）。
2. 把大需求拆成原子子任务，每个子任务必须映射到下列 8 个内置技能之一：
   - 图片分类（输入图片集 → 分类标签）
   - 智能抠图（输入图片+主体 → 透明底图）
   - AI调色（输入图片+风格 → 调色图）
   - 场景生成（输入图+场景描述 → 合成图）
   - 视频粗剪（输入视频素材 → 粗剪草稿）
   - 文案生成（输入主题+平台 → 标题/文案）
   - 合规审核（输入内容+规则 → 通过/不通过+原因）
   - 多平台发布（输入成品+渠道 → 发布回执）
3. 必须包含"合规审核"条件节点，且当不通过时要"打回"到上游重做节点（形成回退边）。
4. 第一个节点永远是"素材入库"触发器；最后一个节点通常是"多平台发布"。
5. 只输出 JSON，不要解释、不要 markdown 代码块包裹，严格符合下方 Schema。
6. 若需求不清，输出 {"clarify": true, "question": "请补充…"} 而不是瞎编。
```

---

## 三、工作流（Workflow）节点设计

在 Coze「工作流」里按以下顺序连线（这是让逻辑"真跑"的关键，比纯人设更可控）：

```
[开始: 用户输入文本]
   ↓
[大模型节点 LLM-Plan]  ← 用第二节的提示词，system 设为上述人设，user 传用户输入
   ↓
[代码节点 FormatJSON]  ← 校验 LLM 输出是否为合法 JSON，剔除可能的 ``` 包裹，失败则走兜底
   ↓
[知识库检索 Node-Template?]（可选）← 若用户提到已知行业，先召回模板中心种子（见第五节）作为 few-shot
   ↓
[结束: 返回生产线 JSON]
```

> 提示：Coze 工作流的「大模型」节点支持设定 system prompt，把第二节内容粘进去即可；"代码节点"用 Python 做 JSON 清洗最稳。

---

## 四、输出 JSON Schema（与 HTML 画布对齐）

LLM 必须输出以下结构（示例为"商品图生产线"）：

```json
{
  "lineName": "商品图生产线",
  "industry": "电商",
  "nodes": [
    {"id":"t","type":"trigger","label":"素材入库","skill":null,"params":{}},
    {"id":"a","type":"action","label":"图片分类","skill":"图片分类","params":{"by":"品类"}},
    {"id":"b","type":"action","label":"智能抠图","skill":"智能抠图","params":{"subject":"商品主体"}},
    {"id":"c","type":"action","label":"AI调色","skill":"AI调色","params":{"style":"抖音风"}},
    {"id":"d","type":"action","label":"场景生成","skill":"场景生成","params":{"scene":"直播间背景"}},
    {"id":"e","type":"condition","label":"合规审核","skill":"合规审核","params":{"rules":["无侵权","无违规"]}},
    {"id":"f","type":"action","label":"多平台发布","skill":"多平台发布","params":{"channels":["抖音","小红书"]}}
  ],
  "edges": [
    {"from":"t","to":"a"},
    {"from":"a","to":"b"},
    {"from":"b","to":"c"},
    {"from":"c","to":"d"},
    {"from":"d","to":"e"},
    {"from":"e","to":"f","label":"通过"},
    {"from":"e","to":"c","label":"打回重做"}
  ],
  "reworkLoop": {"condition":"合规不通过","backTo":"c"}
}
```

字段说明：
- `nodes[].type`：`trigger`(触发) / `action`(执行) / `condition`(条件分支，对应合规审核)
- `nodes[].skill`：必须是第二节 8 个技能之一（trigger 为 null）
- `edges[].label`：仅 condition 出的边需要写"通过/打回重做"
- `reworkLoop`：明确回退条件与回退目标，对应我们原型里的红色打回箭头

---

## 五、知识库 / 模板种子（可选，提升准确率）

在 Coze 建一个「模板中心」知识库，上传以下 3 条作为 few-shot，LLM 召回后拆解更稳：

1. 商品图生产线（电商）：素材入库→图片分类→智能抠图→AI调色→场景生成→合规审核(通过→多平台发布 / 打回→AI调色)
2. 婚纱写真交付（摄影）：素材入库→AI调色→场景生成→生成预览相册→合规审核(通过→客户审片 / 打回→重做)
3. 美食探店图（餐饮）：素材入库→智能抠图→AI调色→场景生成→文案生成→合规审核(通过→多平台发布 / 打回→重写文案)

---

## 六、发布与验证步骤

1. 在 coze.cn 创建智能体，粘贴第一节/第二节内容。
2. 建工作流（第三节），把 LLM 节点 system 设为第二节人设。
3. 测试输入：`帮我搭一条商品图生产线：原图进来先分类，再智能抠图，按抖音规范调色，用 AIGC 补场景，合规不过就打回重做，最后发到各平台`。
4. 检查输出 JSON 是否含 `合规审核` 条件节点 + `打回重做` 回退边。
5. 发布到「网页/API」，拿到可分享链接——这就是你产品的**真实 Agent 能力 demo**（区别于前端 mock）。
6. 进阶：把 Coze 的 API 接回我们 `visionagent-prototype.html` 的"生成工作流"按钮，原型就能从"假数据"升级为"真调用"。

---

## 七、边界说明（面试/汇报时如实讲）

- 本 Coze Agent 解决的是"NL → 生产线 JSON"这一层，**调色/抠图等 Skill 仍是占位**（Coze 可接图像插件，但非必须）。
- 它证明的是"自然语言生成生产线的逻辑可行、拆解可控"，正是我们 PRD 第 11 节里 NL 生成模块的最小可用验证。
- 与前端 mock 的关系是：mock 演示形态，Coze 验证大脑——两者结合才是完整故事。
